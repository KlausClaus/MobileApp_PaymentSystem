package com.example.tuitionpayment.alipay;import android.text.TextUtils;import java.util.Map;public class PayResult {	/**	 * The status of the payment result, e.g., success or failure.	 */	private String resultStatus;	/**	 * The raw result string returned by the payment operation.	 */	private String result;	/**	 * Additional memo information about the payment result.	 */	private String memo;	/**	 * Constructs a {@code PayResult} object by parsing the raw result map returned by Alipay.	 *	 * @param rawResult The raw result map from the payment operation.	 */	public PayResult(Map<String, String> rawResult) {		if (rawResult == null) {			return;		}		// Parse and assign values from the raw result map		for (String key : rawResult.keySet()) {			if (TextUtils.equals(key, "resultStatus")) {				resultStatus = rawResult.get(key);			} else if (TextUtils.equals(key, "result")) {				result = rawResult.get(key);			} else if (TextUtils.equals(key, "memo")) {				memo = rawResult.get(key);			}		}	}	/**	 * Returns a string representation of the payment result.	 *	 * @return A formatted string containing the resultStatus, memo, and result.	 */	@Override	public String toString() {		return "resultStatus={" + resultStatus + "};memo={" + memo				+ "};result={" + result + "}";	}	/**	 * Gets the status of the payment result.	 *	 * @return The payment result status.	 */	public String getResultStatus() {		return resultStatus;	}	/**	 * Gets the memo information from the payment result.	 *	 * @return The memo information.	 */	public String getMemo() {		return memo;	}	/**	 * Gets the raw result string from the payment operation.	 *	 * @return The raw result string.	 */	public String getResult() {		return result;	}}